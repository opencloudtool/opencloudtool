[project]
name = "python-fastapi"

[[project.services]]
name = "app"
image = "ghcr.io/opencloudtool/example-python-fastapi:latest"
internal_port = 8000
external_port = 8000
image = "nginx:latest" # TODO: replace with python-fastapi image
internal_port = 80
external_port = 80
