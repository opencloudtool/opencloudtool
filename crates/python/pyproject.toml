[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "oct"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.maturin]

manifest-path = "oct-py/Cargo.toml"
module-name = "oct._internal"
